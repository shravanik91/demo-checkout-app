{
  "name": "demo-checkout-app",
  "version": "1.0.0",
  "private": true,
  "description": "Small demo repo for AI Release Risk Analyzer PR examples.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
